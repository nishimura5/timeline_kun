# Tests

This directory contains automated tests for TimelineKun.

- `test_app_timer.py`: Verifies key behaviors of the timer application.
- `test_file_loader.py`: Checks file loading/parsing using fixture CSV files.
- `test_smoke.py`: Runs a small smoke test to ensure the app starts and core modules work together.
