{
    "version": "0.2.0",
    "configurations": [
            {
      "name": "Run: timeline_kun (module via uv .venv)",
      "type": "python",
      "request": "launch",
      "module": "timeline_kun",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "justMyCode": true
    }
    ]
}
 